package Common;

public class CommonEnums {

    // TODO

    public enum ExtensionSyncSettingsEnum{
        SHOW_IN_APP,
        MISSED_CALLS,
        MESSAGES
    }

    public enum NavigationTabs{
        INBOX,
        RECENT,
        CALL,
        TEXTS,
        SETTINGS
    }

    public enum InboxTabs{
        INBOX,
        VOICEMAILS,
        FAXES,
        DELETED
    }

    public enum RecentTabs{
        ALL_CALLS,
        MISSED,
        RECEIVED,
        DIALED
    }
}
